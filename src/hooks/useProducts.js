// import { useQuery } from '@apollo/client';
// import { GET_JEANS, GET_DRESSES } from '../graphql/queries';

// export const useProducts = () => {
//   const jeans = useQuery(GET_JEANS);
//   const dresses = useQuery(GET_DRESSES);
//   return { jeans, dresses };
// };
